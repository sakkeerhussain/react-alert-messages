{
  "name": "react-alert-messages",
  "version": "1.0.19",
  "description": "This is a react alert messages helper context",
  "main": "dist/index.js",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && NODE_ENV=production babel src --out-dir dist --copy-files",
    "build:local": "rm -rf dist && NODE_ENV=development babel src --out-dir dist --copy-files && cp package.json dist/package.json",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sakkeerhussain/react-alert-messages.git"
  },
  "keywords": [
    "React",
    "Alert mssages",
    "Alert"
  ],
  "author": "Sakkeer Hussain",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/sakkeerhussain/react-alert-messages/issues"
  },
  "homepage": "https://github.com/sakkeerhussain/react-alert-messages#readme",
  "devDependencies": {
    "@babel/cli": "^7.21.0",
    "@babel/core": "^7.21.0",
    "@babel/preset-env": "^7.20.2",
    "@babel/preset-react": "^7.18.6",
    "prettier": "^2.8.7"
  },
  "peerDependencies": {
    "@babel/polyfill": ">=7.12.1",
    "@emotion/react": ">=11.10.6",
    "@emotion/styled": ">=11.10.6",
    "@mui/material": ">=5.11.12",
    "lodash": ">=4.17.21",
    "react": ">=17.0.0"
  }
}
